# Git — Rust

Rust-specific examples for the [git](../) specifications.

## Commit Scope

From the [commit scope](../commit-scope.md) rules:

`chore(workspace)` commits may touch `Cargo.toml` when updating
workspace-wide dependency versions or adding new workspace members.
